### 项目说明

wendaoiotPannel是wendaoiot开发的通用物联网系统SAAS。具有以下功能：

1、多租户，即除了wendaoiot的管理员外，其它B端客户都可以有自己的管理后台，可以在后台配置一些信息以快速接入设备到物联网系统中。

2、多项目，每个B端客户都可以创建多个项目，每个项目可以有在最终用户看来独立的前端WEB页面和微信小程序。每个项目可以对数据标签进行关联，关联后在前端WEB页面和微信小程序上可以显示这些标签的值，控制类也一样。可以对整个项目进行统一OTA。

3、多设备，每个项目可以接入多个设备，每个设备都有自己的设备ID，设备ID可以是设备的MAC地址、SN码等。设备上报下发采用标准协议，可以对单个设备进行OTA。

4、通讯协议暂时只支持MQTT协议。

5、数据标签，每个设备都可以配置多个数据标签和公式，数据标签关联设备上对应的物理接口，比如温度(temp)关联AI0，代表温度关联AI0接口，通过计算后得到最终值并显示。

6、用户角色：
   - 超级管理员：平台方，管理所有租户。
   - 租户管理员：B端客户，管理自己租户下的项目、设备。不需要多级子账号。

7、设备数据流：
   - 上行：设备 → MQTT Broker → 后端解析存储 → 前端H5/小程序展示
   - 下行：前端H5/小程序 → 后端 → MQTT Broker → 设备

8、技术栈：
   - 后端：Go
   - 前端管理后台：Vue3 + Element Plus
   - C端展示页：H5
   - 小程序：微信原生 / uniapp
   - 数据库：MySQL
   - MQTT Broker：EMQX / Mosquitto

9、部署方式：直接编译部署，无需 Docker。

### MQTT 协议设计

**Topic 结构：**

| 方向 | Topic | 说明 |
|------|-------|------|
| 上行请求 | `wendao/{deviceId}/data` | 设备上报数据 |
| 上行响应 | `wendao/{deviceId}/data/ack` | 服务端回复上报结果 |
| 下行请求 | `wendao/{deviceId}/control` | 服务端下发控制指令 |
| 下行响应 | `wendao/{deviceId}/control/ack` | 设备回复执行结果 |

**消息格式：**

设备上报数据（设备→服务端）：
```json
{ "id": "uuid", "ts": 1680000000000, "tags": { "temp": 25.5 } }
```

服务端回复上报（服务端→设备）：
```json
{ "id": "uuid", "code": 0, "msg": "ok" }
```

服务端下发控制（服务端→设备）：
```json
{ "id": "uuid", "ts": 1680000000000, "tags": { "relay1": 1 } }
```

设备回复控制（设备→服务端）：
```json
{ "id": "uuid", "code": 0, "msg": "ok" }
```

**错误码：**
| code | 说明 |
|------|------|
| 0 | 成功 |
| 1 | 设备未注册 |
| 2 | 标签未配置 |
| 3 | 参数错误 |