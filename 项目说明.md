# 项目说明

WendaoIotPannel 是 Wendaoiot（闻道物联）研发的**通用物联网 SaaS 平台**，解决设备端到云端的数据链路：设备经标准 MQTT 接入，平台完成鉴权、解析、存储、监控、远程控制与 OTA 升级，并以多租户方式对外提供服务。已用于智慧水务、智慧农业、智慧养殖、智慧城市等生产场景。

- 后端：Go（Gin + GORM + paho MQTT + JWT），数据库 MySQL，消息中间件 EMQX。
- 前端：管理后台 Vue3 + Element Plus（`web-admin`）；C 端展示 uni-app（H5 + 微信小程序，`web-app`）。
- 设备端：标准 MQTT 协议，不限定模组；合宙 Air780EPM（LuatOS）参考固件见 `hardware/`。

快速开始、部署与测试见 [部署与测试.md](./部署与测试.md)，HTTP 接口清单见 [README.md](./README.md#http-api-接口列表)。

---

## 1. 总体架构

```
┌──────────────┐   MQTT(mqtts://8883 / mqtt://1883)   ┌─────────┐
│ 设备 / 模组   │ ───────────────────────────────────▶ │  EMQX   │
│ Air780EPM 等 │                                       └───┬─────┘
└──────────────┘                                auth/acl 回调│ REST 互踢/WS 事件
                                                       │ $SYS 上下线
┌──────────────┐   HTTPS / WebSocket          ┌────────┴─────────┐
│ web-admin    │ ────────────────┐            │   Go 后端 :8080    │
│ web-app(H5/小程序)│ ────────────┼──────────▶ │  Gin/GORM/paho    │
└──────────────┘                │            └────────┬─────────┘
                                │                     │
                          静态托管 / Nginx 反代        ▼
                                                        ┌────────┐
                                                        │ MySQL  │
                                                        └────────┘
```

关键机制：

- **设备鉴权下沉到 EMQX**：EMQX 通过 HTTP 回调后端 `/api/v1/mqtt/auth`、`/api/v1/mqtt/acl` 完成「一机一密」认证与主题级授权（共享密钥 `X-Auth-Key` 保护回调）。
- **上下线实时性**：后端订阅 EMQX `$SYS/.../clients/.../connected|disconnected`，设备连接/断开即刷新在线状态，并经内部事件总线 → WebSocket 推给前端，无需轮询；另有周期扫描兜底。
- **会话互踢**：同一设备凭据建立第二条连接时，后端经 EMQX REST API 踢掉旧会话（先在 `wendao/{id}/kicked` 通知原因）。
- **分层**：`cmd`（入口/路由）→ `handler`（HTTP/回调）→ `store`（数据访问）→ `model`（模型）；`mqtt`、`protocol`、`evaluate`、`metrics`、`events` 各司其职。

## 2. 多租户与角色模型

平台围绕 **租户 → 项目 → 设备** 三级组织数据，所有业务表带 `tenant_id` 并在查询层强制租户作用域，防止越权（IDOR）。

| 角色 | 标识 | 能力范围 |
| --- | --- | --- |
| 超级管理员 | `super_admin` | 平台方 Wendaoiot：管理租户、用户；可删除任意业务数据；可见全局 |
| 租户管理员 | `tenant_admin` | B 端客户：管理本租户下项目、产品、设备、固件、OTA、标签、控制命令；看不到其它租户 |

- **租户**：一个 B 端客户一个租户；创建租户时自动生成该租户的管理员账号与随机强密码（明文仅返回一次）。
- **项目**：租户下可建多个项目，每个项目对应一套独立的 C 端 H5 / 小程序视图；项目可绑定数据标签、统一 OTA、配置设备在线判定。
- **设备**：归属于项目；设备 ID 为业务自定义字符串（IMEI / MAC / SN 等），**区分大小写**，仅允许 1–64 位字母、数字、下划线、短横线。
- **产品**：一型一密的凭证主体，**租户级**（不绑定具体项目，同批烧录设备可分布到不同项目）。

> 不需要多级子账号，每个租户一个管理员账号即可。

## 3. 核心数据模型

| 模型 | 关键字段 | 说明 |
| --- | --- | --- |
| `AdminUser` | id, username, password(bcrypt), role, tenant_id, token_version, ui_options | 后台用户；`token_version` 失效旧 JWT |
| `Tenant` | id, name(唯一), deleted_at | 租户；删除走全链物理级联 |
| `Project` | id, tenant_id, name, online_mode, offline_timeout_sec | 项目；删除后 ID 复用最低空闲位 |
| `Product` | product_key(唯一), product_secret(bcrypt, 不回显), tenant_id, dyn_reg_enabled | 一型一密产品凭证 |
| `Device` | id(字符串主键), tenant_id, project_id, device_secret(bcrypt), enabled, status, product_id, activated_at, online_mode, offline_timeout_sec, last_active | 设备；`product_id>0 且 secret 为空` = 待激活 |
| `DeviceTag` | tag_key, interface(如 AI0), formula(如 `x*0.1`) | 设备标签映射与公式 |
| `ProjectTag` | project_id, tag_key | 项目绑定展示的标签（引用 DeviceTag） |
| `DeviceData` | device_id, ts(服务端毫秒), device_ts, tags(JSON), 计算值 | 上行数据 |
| `ControlLog` / `ControlCommand` | msg_id, status, … | 控制流水与项目自定义命令 |
| `DeviceMessage` / `DevicePeerAllow` | from/to/payload | D2D 留痕与跨租户白名单 |
| `Firmware` / `OTATask` / `OTALog` | version, https url, target_type, status | OTA 固件、任务、进度日志 |

- 时间：上行一律由服务端落接收时间（毫秒）入库排序，设备时间归一化到 `device_ts`，不信任设备时钟。
- 删除：业务资源删除为物理删除 + 显式级联（避免软删唯一索引冲突与孤儿数据）；`deleted_at` 仅在部分模型保留。
- 建表：后端启动时 `AutoMigrate` 自动建表/加列，无需手工 SQL。

## 4. 数据采集与标签公式

- 每个设备可配置多个**数据标签**，把标签键绑定到设备物理接口并配置公式，例如温度 `temp → AI0 → x*0.1`，湿度 `humidity → AI1`（公式留空表示原值）。
- 项目可**绑定标签**，绑定后该项目的 C 端 H5 / 小程序才展示对应标签的值与控制项。
- 上行 `tags` 经 `internal/evaluate` 公式引擎计算后随原始值一起存储，前端展示计算结果。

## 5. MQTT 协议

### 5.1 Topic 总表

前缀固定 `wendao/`，第二段是设备 ID（`{id}`）。`{id}` 是占位符，实际使用必须替换为自己的设备 ID；**原样订阅带花括号的主题会被 ACL 拒绝并断开**。

| 方向 | Topic | 权限 | 说明 |
| --- | --- | --- | --- |
| 上行 | `wendao/{id}/data` | 设备 publish | 上报数据 |
| 下行 | `wendao/{id}/data/ack` | 设备 subscribe | 上报应答 |
| 下行 | `wendao/{id}/control` | 设备 subscribe | 平台下发控制 |
| 上行 | `wendao/{id}/control/ack` | 设备 publish | 设备控制回执 |
| 下行 | `wendao/{id}/control/ack/resp` | 设备 subscribe | 平台对回执的确认 |
| 下行 | `wendao/{id}/ota` | 设备 subscribe | OTA 任务推送 |
| 上行 | `wendao/{id}/ota/progress` | 设备 publish | OTA 进度 |
| 上行 | `wendao/{id}/ota/ack` | 设备 publish | OTA 结果 |
| 下行 | `wendao/{id}/ping` | 设备 subscribe | ping 探活（ping 模式） |
| 上行 | `wendao/{id}/ping/ack` | 设备 publish | 探活应答 |
| 上行 | `wendao/{id}/status` | 设备 publish | 在线遗嘱/状态 |
| 上行 | `wendao/{id}/peer` | 设备 publish | D2D 发信请求 |
| 下行 | `wendao/{id}/peer/ack` | 设备 subscribe | D2D 投递回执 |
| 下行 | `wendao/{id}/inbox` | 设备 subscribe | D2D 收件箱 |
| 下行 | `wendao/{id}/kicked` | 设备 subscribe | 被踢原因通知（QoS1） |
| 引导 | `wendao/register/{sn}/req` | 引导连接 publish | 一型一密注册请求 |
| 引导 | `wendao/register/{sn}/reply` | 引导连接 subscribe | 一型一密注册应答 |

**ACL 规则**：

- 发布（publish）为**严格白名单**：仅允许上表 publish 方向后缀（`data`、`control/ack`、`ota/ack`、`ota/progress`、`ping/ack`、`status`、`peer`，以及引导期的 `register/{sn}/req`）。伪造他人主题一律拒绝。
- 订阅（subscribe）为**本人前缀全放行**：第二段等于自己设备 ID 的任意主题（含 `#`/`+` 通配）均可订阅；他人前缀、`$SYS`、未替换占位符一律拒绝。
- 授权策略 `no_match=deny`、`deny_action=disconnect`：违规发布/订阅会被立即断开。

### 5.2 基础报文

设备上报（设备 → 平台），发布到 `wendao/{id}/data`：

```json
{ "id": "uuid-001", "ts": 1680000000000, "tags": { "temp": 255, "humidity": 60 } }
```

平台应答（`wendao/{id}/data/ack`）：

```json
{ "id": "uuid-001", "code": 0, "msg": "ok" }
```

平台下发控制（`wendao/{id}/control`）：

```json
{ "id": "msg-uuid", "ts": 1680000000000, "tags": { "relay1": 1 } }
```

设备回执（`wendao/{id}/control/ack`），平台随后在 `control/ack/resp` 回 `{code:0}`：

```json
{ "id": "msg-uuid", "code": 0, "msg": "ok" }
```

> **先订阅应答主题，再发布数据**。应答按 QoS1、clean session 转发，先发布后订阅会错过应答（MQTTX 调试最常见问题）。

### 5.3 错误码

| code | 宏 | 说明 |
| --- | --- | --- |
| 0 | success | 成功 |
| 1 | device_not_registered | 设备未注册/被禁用 |
| 2 | tag_not_configured | 标签未配置 |
| 3 | param_error | 参数错误（缺 id/to、未知 msg_id、非法 JSON 等） |
| 4 | already_activated | 一型一密设备已激活（不能重复引导注册） |
| 5 | device_offline | D2D 目标设备离线；或对离线设备强下发 |
| 6 | forbidden | 跨租户且无白名单授权 |
| 7 | peer_not_found | D2D 目标设备不存在 |

### 5.4 设备接入鉴权（两种方式）

#### 一机一密（每设备独立密钥）

1. 管理后台「新建设备」或调用 `POST /api/v1/devices`，平台生成 `device_secret`，**明文仅返回一次**。
2. 设备 MQTT 连接：`username = 设备ID`，`password = device_secret`。
3. 「重置密钥」生成新密钥：**不影响已建立的连接**（连接在 CONNECT 时完成认证），但旧密钥立即无法新建连接；新密钥连接时会互踢残留旧会话。
4. 设备 ID 与用户名严格区分大小写（库列为 `utf8mb4_bin`）。

#### 一型一密（产品级密钥 + 动态注册）

适合批量烧录。先用产品凭证引导换发一机一密，再用一机一密正常通信。

1. 平台创建产品，得到 `product_key` / `product_secret`（secret 仅创建/重置时返回一次，列表不回显）。
2. 通过批量预录接口或「录入设备」把 SN 预录到产品下（设备进入**待激活**态：`product_id>0 且 device_secret 为空`）。
3. 设备用**引导连接**接入：
   - MQTT `username = "{SN}&{ProductKey}"`，`password = product_secret`；
   - 引导连接仅允许 `wendao/register/{sn}/req`（发）与 `wendao/register/{sn}/reply`（收），不能访问业务主题，也不参与互踢/在线统计。
4. 设备发布注册请求：

   ```json
   { "id": "reg-1", "sn": "DEVICE_SN", "product_key": "XXXXXXXX" }
   ```

   成功应答（随后平台异步踢除引导连接）：

   ```json
   { "id": "reg-1", "code": 0, "msg": "ok", "device_secret": "<一机一密，仅一次>" }
   ```

5. 设备保存 `device_secret`，断开引导连接，用「一机一密」方式（username=SN，password=device_secret）重连并上报数据，平台置「已激活」。
6. 已激活设备再次引导注册返回 `code=4` 并拒绝；管理员可「重置激活（reactivate）」清空密钥回待激活态（会踢线）。
7. 产品可开关动态注册（关闭后引导认证直接拒绝）；产品下仍有设备引用时不允许删除产品。

### 5.5 控制指令状态机

```
pending ──发布成功──▶ delivered ──设备 ack code=0──▶ success
   │                    │              └─ack code≠0─▶ failed
   │                    └──── 30s 无 ack ──────────▶ timeout
   └─ 设备离线/失败 ───────────────────────────────▶ failed
```

前端可用 `GET /api/v1/devices/:id/control/:msgId` 轮询，也可经 WebSocket 接收 `control_ack` 实时事件。

### 5.6 设备间通信（D2D）

设备 A 发布到 `wendao/A/peer`，由平台校验并投递给目标的 `wendao/{to}/inbox`：

```json
{ "id": "peer-1", "ts": 1725700000000, "to": "B", "type": "notify", "payload": { "text": "hello" } }
```

`to` 的目标语义：

| 取值 | 含义 |
| --- | --- |
| `"B"` | 点对点；同租户默认允许，跨租户需白名单 |
| `"B,C,D"` | 多播，去重，逐目标校验，回执 `delivered n/m` |
| `"*"` | 租户广播：同租户全部在线启用设备，排除自己 |
| `"project:{id}"` | 项目广播（跨租户项目回 code=6） |

回执经 `wendao/A/peer/ack`：`{id, code, msg}`，`msg` 形如 `delivered 1/1`（单目标）或 `delivered 2/3`（多播）。单次广播目标上限 500。平台只做**在线投递**，目标离线回 code=5，不做离线暂存。跨租户需超管调用 `POST /api/v1/peer/allows` 授权。所有收发（含失败）写入 `device_messages` 可查。

### 5.7 会话互踢与被踢通知

同一设备凭据建立第二条连接时，平台认证通过后：先向 `wendao/{id}/kicked` 发布 QoS1 通知，再通过 EMQX REST API 删除该 username 的旧会话：

```json
{ "reason": "new_login", "msg": "同一设备的新连接已通过认证，本连接被平台断开（互踢）", "ts": 1725000000000 }
```

`clean session=false` 的设备重连后可收到该通知。其它被踢原因：`disabled`（管理员禁用设备）、`reactivated`（重置激活）。互踢依赖 EMQX REST API Key（`emqx.api_key/secret`），未配置时该能力静默禁用（仅日志）。

## 6. 设备在线判定

全局默认在 `config.yaml` 的 `device.online_mode` 配置，且**每台设备可单独覆盖**（项目设置可批量套用默认值）：

| 模式 | 判定方式 | 适用 |
| --- | --- | --- |
| `connection`（默认） | 仅按 MQTT 连接：连上即在线、断开即离线；周期扫描不回收（broker 宕机事件丢失时兜底） | 通用 |
| `report` | 在 `offline_timeout_sec` 内有数据上报即在线；连接事件仍即时置位，超时补充连接假死 | 按上报周期保活的设备 |
| `ping` | 平台每 `ping_interval_sec` 发 `wendao/{id}/ping`，设备回 `ping/ack` 刷新活跃时间，超时未应答由扫描判离线 | 常供电常连接设备（休眠设备不适用） |

## 7. OTA 升级

- **固件**：`POST /api/v1/firmwares` 登记固件（名称、版本、**下载地址必须 https**、md5、size）；支持按设备/版本查询最新可用固件。
- **任务**：`POST /api/v1/ota/tasks` 创建升级任务，目标类型 `device`（单设备）或 `project`（项目内全部启用设备）。
- **流程**：平台向 `wendao/{id}/ota` 推送 `{type:"ota", version, url, md5, size}` → 设备在 `ota/progress` 上报进度（`downloading` / `installing` + 百分比）→ 在 `ota/ack` 上报成功/失败，驱动 `ota_logs` 状态机与任务 `done` 收敛。
- 越权（非本租户设备）、非法固件、非法 target_type 均拒绝；OTA 日志支持按行/按任务/清空删除（仅超管）。

## 8. 实时 WebSocket

- 握手：`GET /api/v1/ws?token=<JWT>`（浏览器 WS 无法自定义 Authorization 头，故用 query 传 token，握手时校验 `token_version`），也支持 `Sec-WebSocket-Protocol` 携 token。
- 服务端按租户广播事件，前端用订阅方式接收，主要事件类型：
  - `device_status`：设备上下线/禁用（含 `online`、`enabled`、`status`）。
  - `device_activated`：一型一密设备激活（含 `device_id`、`product_id`、`activated_at`）。
  - `control_ack`：控制指令状态变化。
  - `ota_progress` / `ota_ack`：OTA 进度与结果。
  - `device_peer`：D2D 消息。

## 9. 安全设计要点

- 密码一律 **bcrypt** 加盐存储；JWT 带 `token_version`，改密/删用户后旧 token 立即失效；认证中间件每请求查库。
- 设备一机一密（bcrypt 校验）+ EMQX HTTP auth/acl 主题授权 + TLS（推荐 8883 mqtts）；默认关闭 1883 明文或仅过渡期保留。
- 路由级角色强制（超管接口 `RequireRole(super_admin)`）+ 查询层租户作用域，阻断 IDOR。
- EMQX auth/acl 回调用共享密钥 `mqtt_auth_secret`（`X-Auth-Key`）校验，非设备密钥。
- 敏感配置（DB 密码、JWT 密钥、EMQX 密钥）通过配置文件或 `WQ_` 环境变量注入，`config.yaml` 不入库；生产严禁使用默认弱密钥（可用 `WQ_INSECURE=1` 仅在本地跳过强度校验）。
- CORS 生产环境必须配置白名单 `server.cors_origins`。

## 10. 前端说明

- `web-admin`（管理后台，Vue3 + Element Plus + Vite，dev :3000）：登录、仪表盘（设备状态/消息监控，纯 SVG 可视化）、租户/用户/项目/产品/设备/固件/OTA/控制日志管理，WebSocket 实时刷新，支持每账号 UI 偏好持久化。
- `web-app`（C 端，uni-app，dev :3001）：按项目展示绑定标签的实时值与控制，H5 与微信小程序同构。
- 构建：管理后台 `npm run build`（产物 `dist/`）；C 端 `npm run build:h5` / `npm run build:mp-weixin`。

## 11. 参考设备端固件（hardware/）

平台设备侧是标准 MQTT（`wendao/{deviceId}/...`），任何支持 MQTT 的模组/MCU 均可接入。`hardware/780epm_common/` 是合宙 Air780EPM（LuatOS）的参考工程：

- `project/` 下含 `demo_0_5v`、`demo_4_20ma`、`demo_panel`、`demo_rs485`、`demo_ttl`（基础接口 demo）与 `pannel_demo`、`pannel_demo_sleep`、`pannel_demo_no_sleep`、`pannel_demo_tcp_shortsleep`（完整面板/休眠场景），各自带说明文件。
- 默认连接 `pannel.wendaoiot.com:1883`，设备身份取模组 IMEI；接自有平台时改 demo 内 `mqtt_wendao.lua` 顶部 `HOST`/`PORT`/`TOPIC_PREFIX`，并按第 5 节实现密钥与主题。
- 烧录用 Luatools，选择 `core/` 下官方 `.soc` 固件与工程脚本一起下载。
