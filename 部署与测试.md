# 部署与测试

本文档介绍 WendaoIotPannel 的本地联调、生产部署、配置项、EMQX/TLS 对接以及测试方法。系统设计与协议见 [项目说明.md](./项目说明.md)，接口清单见 [README.md](./README.md#http-api-接口列表)。

- 后端：Go 1.25+，单二进制，监听 `:8080`。
- 依赖：MySQL 8.0、EMQX 5.x。
- 前端：`web-admin`（管理后台）、`web-app`（C 端 H5/小程序），均为静态产物，用 Nginx 托管并反代 API。

---

## 一、本地联调

### 1.1 依赖

- Go 1.25+、Node.js 18+、MySQL 8.0、EMQX 5.x；可选 Docker Desktop。
- 自动化测试另需 Python 3.10+ 与 `paho-mqtt`（`pip install paho-mqtt`）。

### 1.2 Docker Compose 起依赖（推荐，跨平台）

已入库的 [`deploy/docker/`](./deploy/docker/) 提供 MySQL + EMQX 容器栈，无需额外脚本：

```bash
cd deploy/docker
cp .env.example .env          # 修改其中所有 change_me 密码
./gen-certs.sh                # 生成 8883 证书（Windows 用 gen-certs.ps1）
docker compose up -d
./configure-emqx.sh           # 后端起来后配置认证/ACL（Windows 用 configure-emqx.ps1）
```

详见 [`deploy/docker/README.md`](./deploy/docker/README.md)。

### 1.3 Windows 一键启动（维护者本地脚本）

> `tools/local/` 目录被 `.gitignore` 忽略，属于维护者本机脚本（含 `up.bat`、带真实密钥的 compose/emqx.conf/证书），克隆仓库者请用 1.2。

```bat
tools\local\up.bat
```

脚本会：起 MySQL + EMQX 容器 → 等待健康 → 调 EMQX REST 配置 HTTP 认证/ACL 链、超管账号、API Key → 启动后端（:8080）与管理后台（:3000）。

- 管理后台：http://localhost:3000 ，超管 `admin / admin123`
- EMQX Dashboard：http://localhost:18083 （账号见本地 `docker-compose.yml`）

### 1.4 通用步骤（无一键脚本 / 其它平台）

1）建库：

```sql
CREATE DATABASE IF NOT EXISTS wendaoiot DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

2）准备配置：

```bash
cd server
cp config.example.yaml config.yaml      # Windows: copy config.example.yaml config.yaml
```

按实际填写 MySQL、`jwt_secret`、`mqtt_auth_secret`、`emqx` 段；本地联调可用 `WQ_INSECURE=1` 跳过密钥强度校验（**生产严禁**）。表结构由后端启动时 `AutoMigrate` 自动创建。

3）启动 EMQX 并配置认证/ACL（见下文第三节），或本地先用无认证 broker 联调（仅限功能验证，切勿用于生产）。

4）启动后端：

```bash
cd server
go run ./cmd/server/
# 或编译
go build -o wendao-server ./cmd/server/ && ./wendao-server
```

健康检查：

```bash
curl http://localhost:8080/api/v1/health
# {"code":0,"msg":"ok","data":{"status":"ok",...}}
```

5）启动前端：

```bash
cd web-admin && npm install && npm run dev      # http://localhost:3000
cd web-app   && npm install && npm run dev:h5   # http://localhost:3001/iot/
```

### 1.5 手工冒烟（MQTTX）

1. 管理后台「设备管理 → 新建设备」，记录弹窗中的 `device_secret`（仅显示一次）。
2. MQTTX 连接 `mqtt://127.0.0.1:1883`：用户名=设备ID，密码=`device_secret`。
3. **先订阅** `wendao/<设备ID>/data/ack`，再向 `wendao/<设备ID>/data` 发布：

   ```json
   { "id": "t1", "ts": 1700000000000, "tags": {} }
   ```

   收到 `{"code":0,"msg":"ok"}` 即链路打通，后台设备变「在线」。

---

## 二、配置项说明

配置文件默认 `server/config.yaml`，可用环境变量 `WQ_CONFIG_PATH` 指定其它路径。所有敏感项均可用 `WQ_` 前缀环境变量覆盖（优先级高于文件）。

| 配置键 | 环境变量 | 说明 |
| --- | --- | --- |
| `server.port` | `WQ_SERVER_PORT` | HTTP 端口，默认 8080 |
| `server.jwt_secret` | `WQ_JWT_SECRET` | JWT 签名密钥，**生产必填**强随机（建议 `openssl rand -base64 48`） |
| `server.mqtt_auth_secret` | `WQ_MQTT_AUTH_SECRET` | EMQX auth/acl 回调共享密钥，须与 EMQX 端 `X-Auth-Key` 一致 |
| `server.cors_origins` | `WQ_CORS_ORIGINS` | 允许的前端来源，逗号分隔；留空仅放行本机 3000/3001。**生产必须配白名单** |
| `mysql.host/port/user/password/database` | `WQ_MYSQL_*` | MySQL 连接 |
| `mqtt.broker` | `WQ_MQTT_BROKER` | 后端连接 broker 地址，如 `tcp://127.0.0.1:1883` |
| `mqtt.client_id/username/password` | `WQ_MQTT_CLIENT_ID/USERNAME/PASSWORD` | 后端自身连接账号（区别于设备密钥） |
| `device.online_mode` | `WQ_DEVICE_ONLINE_MODE` | `connection`（默认）/`report`/`ping` |
| `device.offline_timeout_sec` | `WQ_DEVICE_OFFLINE_TIMEOUT_SEC` | 离线超时秒数 |
| `device.scan_interval_sec` | `WQ_DEVICE_SCAN_INTERVAL_SEC` | 离线扫描周期 |
| `device.ping_interval_sec` | `WQ_DEVICE_PING_INTERVAL_SEC` | ping 模式探活周期 |
| `emqx.api_base` | `WQ_EMQX_API_BASE` | EMQX REST/Dashboard 地址，如 `http://127.0.0.1:18083` |
| `emqx.api_key` / `emqx.api_secret` | `WQ_EMQX_API_KEY` / `WQ_EMQX_API_SECRET` | 设备会话互踢用 REST 凭据；留空则互踢静默禁用 |

`config.yaml` 已在 `.gitignore` 中，**不要把含真实密码的配置提交到公开仓库**；`config.example.yaml` 是可提交的模板。

---

## 三、EMQX 对接（认证 / ACL / 互踢 / TLS）

### 3.1 HTTP 认证与授权回调

后端提供两个受共享密钥保护的回调：

- 认证：`POST http://<后端>:8080/api/v1/mqtt/auth`
  - 头：`X-Auth-Key: <mqtt_auth_secret>`
  - 体：`{"clientid":"...","username":"<设备ID>","password":"<device_secret>"}`
  - 成功 `{"result":"allow"}`，失败 `{"result":"deny","reason":"..."}`
- 授权：`POST http://<后端>:8080/api/v1/mqtt/acl`
  - 头同上；体：`{"username":"<设备ID>","topic":"...","action":"subscribe|publish"}`
  - 允许 `{"result":"allow"}`，拒绝 `{"result":"deny"}`

EMQX 5 Dashboard 配置：

1. **访问控制 → 认证（Authentication）→ 添加 → Password-Based → HTTP Server**：POST 到 `/mqtt/auth`，请求头加 `X-Auth-Key`，请求体携带 `clientid/username/password`，以响应 JSON 的 `result == "allow"` 判定。
2. **访问控制 → 授权（Authorization）→ 添加 → HTTP**：URL 指向 `/mqtt/acl`，同样带 `X-Auth-Key`，体携带 `username/topic/action`。
3. 授权设置：`no_match = deny`、`deny_action = disconnect`、关闭授权缓存（保证密钥重置即时生效）。
4. 后端 `config.yaml` 设同一 `mqtt_auth_secret`。

### 3.2 设备会话互踢（EMQX REST API Key）

同一设备重复登录需要 REST 凭据：Dashboard → **访问控制 → API 密钥** 新建，把返回的 key/secret 填入后端 `emqx.api_key/api_secret`（或 `WQ_EMQX_API_*`）。

> EMQX 5.8.4 注意：创建 API Key 的 `POST /api/v5/api_key`（**单数**）请求体必须**同时**带
> `name`、`desc`、`enable`、`expired_at` 四字段，缺 `desc`/`enable` 会 500（badmatch）；
> 删除用 `/api/v5/api_key/{name}`；api_key 由服务端生成，secret 仅返回一次。
> 三项任一未配置时互踢静默禁用（仅日志，不影响认证）。

### 3.3 TLS（推荐 8883 mqtts）

- 用 OpenSSL 生成自签 CA 与服务器证书，SAN 需包含实际访问主机名（如 `DNS:pannel.wendaoiot.com,IP:127.0.0.1,DNS:localhost`），否则主机名校验失败。
- EMQX 5.8 启用 SSL listener 推荐挂载 `emqx.conf`（深层 `EMQX_LISTENERS__SSL__...` 环境变量不被识别）：

  ```hocon
  listeners.ssl.default {
    enabled = true
    bind = "0.0.0.0:8883"
    ssl_options {
      cacertfile = "/opt/emqx/certs/ca.crt"
      certfile   = "/opt/emqx/certs/server.crt"
      keyfile    = "/opt/emqx/certs/server.key"
      verify     = verify_none
    }
  }
  ```
- 设备/MQTTX 使用 `mqtts://<host>:8883` 并导入 CA 证书；1883 明文建议仅在受信网络过渡期保留。
- OTA 固件下载地址在创建固件时被强校验为 `https://`。

---

## 四、生产部署（Linux + systemd + Nginx 示例）

> **想省事**：无 Docker 的 Linux 服务器可直接用已入库脚本一键完成安装配置：
> ```bash
> sudo bash deploy/linux/deploy.sh          # 详见 deploy/linux/README.md
> ```
> 以下为手工单机参考拓扑：Nginx（443/80）托管两个前端静态产物并反代后端；后端以 systemd 常驻；MySQL、EMQX 用本机进程（或见 1.2 的容器方式）。请把所有 `CHANGE_ME`、域名、密码替换为生产值。

### 4.1 构建后端（Linux amd64）

```bash
# 在项目根的 server/ 下
CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -trimpath -ldflags="-s -w" -o wendao-server ./cmd/server/
```

部署目录示例：`/opt/wendao/server/`（放 `wendao-server` 与 `config.yaml`）。敏感项优先用环境变量注入。

`/etc/systemd/system/wendao.service`：

```ini
[Unit]
Description=WendaoIotPannel backend
After=network.target mysql.service

[Service]
Type=simple
WorkingDirectory=/opt/wendao/server
EnvironmentFile=/opt/wendao/server/wendao.env
ExecStart=/opt/wendao/server/wendao-server
Restart=always
RestartSec=3
User=wendao

[Install]
WantedBy=multi-user.target
```

`wendao.env`（权限 600，示例）：

```ini
WQ_JWT_SECRET=<openssl rand -base64 48 的输出>
WQ_MQTT_AUTH_SECRET=<与 EMQX X-Auth-Key 一致的强随机>
WQ_MYSQL_HOST=127.0.0.1
WQ_MYSQL_USER=wendaoiot
WQ_MYSQL_PASSWORD=<强密码>
WQ_MYSQL_DATABASE=wendaoiot
WQ_MQTT_BROKER=tcp://127.0.0.1:1883
WQ_EMQX_API_BASE=http://127.0.0.1:18083
WQ_EMQX_API_KEY=<api_key>
WQ_EMQX_API_SECRET=<api_secret>
WQ_CORS_ORIGINS=https://pannel.wendaoiot.com
```

启用：

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now wendao
sudo systemctl status wendao
curl http://127.0.0.1:8080/api/v1/health
```

首次启动自动建表并播种超管。**上线后立即修改超管与租户管理员默认密码**（默认 `admin/admin123`）。

### 4.2 构建前端

```bash
# 管理后台
cd web-admin
npm install
npm run build           # 产物 dist/

# C 端 H5
cd ../web-app
npm install
npm run build:h5        # 产物 dist/build/h5
# 微信小程序：npm run build:mp-weixin，用微信开发者工具上传
```

如 API 不是同源反代，需在前端构建时配置 API 基地址/代理（管理后台开发代理见 `web-admin/vite.config.ts`，生产建议同源反代，避免跨域）。

### 4.3 Nginx 托管 + 反代 + WebSocket

```nginx
server {
    listen 443 ssl;
    server_name pannel.wendaoiot.com;

    ssl_certificate     /etc/nginx/certs/pannel.crt;
    ssl_certificate_key /etc/nginx/certs/pannel.key;

    # 管理后台
    location / {
        root /opt/wendao/web-admin;
        try_files $uri $uri/ /index.html;
    }

    # C 端 H5（示例子路径；按实际 base 调整）
    location /iot/ {
        alias /opt/wendao/web-app-h5/;
        try_files $uri $uri/ /iot/index.html;
    }

    # API
    location /api/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # WebSocket 实时推送
    location /api/v1/ws {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 3600s;
    }
}
server { listen 80; server_name pannel.wendaoiot.com; return 301 https://$host$request_uri; }
```

### 4.4 MySQL / EMQX / 防火墙

- MySQL：建库 `wendaoiot`（utf8mb4），创建专用账号、最小授权；仅监听内网或本机，不对公网暴露 3306。
- EMQX：对外仅放行业务必需端口——1883（如保留明文）/8883（mqtts，推荐）；Dashboard 18083、REST 不要暴露公网，或加访问控制。
- 后端 8080 仅监听本机/内网，由 Nginx 反代。
- 上线安全清单：
  - [ ] 更换 `jwt_secret`、`mqtt_auth_secret`、MySQL 密码、EMQX Dashboard/API 凭据；
  - [ ] 修改超管默认密码，租户初始密码分发后要求修改；
  - [ ] `cors_origins` 配置确切域名白名单；
  - [ ] 启用 8883 TLS，设备侧导入 CA；评估关闭 1883；
  - [ ] 确认 EMQX auth/acl 回调生效（错误凭据 rc=5、越权主题被断开）；
  - [ ] 健康检查与接口冒烟通过（见第五节）。

---

## 五、测试

### 5.1 自动化套件（Python + paho-mqtt）

测试脚本维护在本地 `tools/local/mqtt-test/`（git 忽略目录；核心断言亦可据此在你自己的环境复现）。前置：MySQL/EMQX 已起、后端 :8080 运行，EMQX auth/acl 已按第三节配置。

| 套件 | 命令 | 用例 | 覆盖 |
| --- | --- | --- | --- |
| MQTT 冒烟基础 | `python mqtt_e2e_test.py --only smoke` | — | 登录、容器、在线状态连通性 |
| MQTT 用例 | `python mqtt_e2e_test.py --only cases` | — | 上报/控制闭环、大小写、ACL 拒绝、未注册拒绝 |
| D2D | `python mqtt_e2e_test.py --only d2d` | — | 点对点/离线/不存在/跨租户/留痕 |
| 新特性 | `python mqtt_e2e_test.py --only new5` | 6 | 多播/项目广播/租户广播/跨租户拒绝/互踢/项目 ID 复用 |
| MQTT 全量 | `python mqtt_e2e_test.py --only all` | 22 | 上述合集 |
| ACL 探针 | `python acl_probe.py` | 11 | 本人前缀订阅放行、通配放行、他人/占位符/`$SYS` 拒绝、发布白名单 |
| API 全量 | `python api_e2e_test.py --only all` | 76 | 一机一密 14 / 一型一密 34 / OTA 28 |

`api_e2e_test.py` 自动建独立租户/项目/设备/产品/固件，结束自动清理（`--keep` 可保留现场）。踢线相关用例依赖 EMQX API Key 有效，失效时表现为踢线用例 FAIL（此时按 3.2 重建 Key 并同步后端配置后重启）。

最近一次全量回归（2026-09-18）：**API 76/76、MQTT 22/22、ACL 11/11 全部通过**；`go vet`/`go test`/`go build`、web-admin `vue-tsc` 与 `vite build`、web-app `build:h5` 均通过。

### 5.2 后端与前端构建校验

```bash
cd server && go vet ./... && go test ./... && go build ./cmd/server/
cd ../web-admin && node node_modules/vue-tsc/bin/vue-tsc.js --noEmit && npm run build
cd ../web-app && npm run build:h5
```

### 5.3 核心功能手工清单

- [ ] 登录：正确密码进入；错误密码提示；超管选「租户管理员」角色登录被拒（role mismatch）。
- [ ] 未登录访问受保护页面/接口 → 跳登录 / 401。
- [ ] 租户：超管新建租户、获得租户管理员初始账号；该账号只见本租户数据。
- [ ] 项目/设备：建项目 → 新建设备（一机一密）或产品录入（一型一密待激活）。
- [ ] 设备上线：正确凭据连接即在线；错误/空/未注册/大小写错误凭据被拒（rc=5）。
- [ ] 数据上报：先订阅 `data/ack` 再发，收 code=0；标签公式计算正确（如 `temp→AI0→x*0.1`）、历史分页正常。
- [ ] 控制：HTTP 下发 → 设备收 `control` → 回 `control/ack` → 状态 success；不回 ack 30s 超时；离线设备下发被拒。
- [ ] 在线状态：CONNECT/DISCONNECT 无需刷新即时变化（WebSocket）。
- [ ] 互踢：同设备第二条连接登录，第一条收到 `kicked` 通知后被断开。
- [ ] 一型一密：引导 `SN&ProductKey` 注册换发一机一密；重复激活 code=4；reactivate 回待激活并踢线。
- [ ] D2D：单播/多播/`*`/`project:{id}` 投递与 code 5/6/7；跨租户需白名单。
- [ ] OTA：固件必须 https；设备级与项目级任务；progress/ack 驱动日志与任务 done；失败状态正确。
- [ ] 权限：租户管理员看不到「租户管理」及其它租户；超管删数据/删日志接口对租户管理员拒绝。
- [ ] 删除：删租户/项目/设备后无孤儿数据，同 ID 可重新创建（ID 复用最低空闲位）。
- [ ] C 端 H5：租户管理员登录后按项目展示绑定标签、实时刷新（3s）、可下发控制并收到 ack。
