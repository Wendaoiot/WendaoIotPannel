# 设备接入指南

> 本文面向**设备固件 / 端侧开发者**，只回答三个问题：设备**发什么**、**收什么**、被点名后**怎么应答**。
> 不涉及后台管理、REST API、服务端部署（这些见运维文档）。
> 所有主题、报文、错误码均与平台代码一致，可直接照抄。下文中 `{deviceID}` 一律替换为**你自己的设备 ID**。

---

## 目录

1. [建立连接](#1-建立连接)
2. [通用约定（务必先读）](#2-通用约定务必先读)
3. [数据上行（设备核心）](#3-数据上行设备核心)
4. [平台指令下行（设备核心）](#4-平台指令下行设备核心)
5. [在线 ping 探活](#5-在线-ping-探活)
6. [OTA 固件升级](#6-ota-固件升级)
7. [设备间通信 D2D（可选）](#7-设备间通信-d2d可选)
8. [被踢通知与连接异常排查](#8-被踢通知与连接异常排查)
9. [订阅与发布权限汇总](#9-订阅与发布权限汇总)
10. [附录：错误码与自检清单](#10-附录错误码与自检清单)

---

## 1. 建立连接

### 1.1 连接参数

| 参数                 | 值                                                          |
| -------------------- | ------------------------------------------------------------ |
| Broker（明文）       | `tcp://pannel.wendaoiot.com:1883`                            |
| Broker（加密，推荐） | `mqtts://pannel.wendaoiot.com:8883`                          |
| Username             | **设备 ID**（小写示例 `test_001`）                           |
| Password             | **设备密钥 device_secret**（管理后台创建设备时只显示一次）   |
| MQTT 版本            | 3.1.1 或 5.0 均可                                            |
| Client ID            | 任意，**不参与认证**；建议 = 设备 ID 或 `设备ID_随机后缀` |
| Keep Alive           | 建议 30–60 秒                                               |

### 1.2 设备 ID 规则（重要）

- 仅允许 **1–64 位**：字母、数字、下划线 `_`、短横线 `-`（正则 `^[A-Za-z0-9_-]{1,64}$`）。
- **严格区分大小写**：`test_001` 与 `TEST_001` 是两台不同设备。用错大小写会认证失败。
- 用户名（设备 ID）与设备密钥由平台在设备入网/重置时签发；设备密钥请安全存储在 Flash/NVRAM。

### 1.3 TLS（8883）

- 生产环境推荐走 `8883`。平台证书：
  - 若使用**受信任 CA 签发**的证书，直接连接即可；
  - 若使用**自签 CA**，设备需预置平台提供的 `ca.crt` 并校验，不能跳过证书校验（仅调试时可临时关闭）。
- 首次联调可用明文 1883，正式产品请用 8883。

### 1.4 Clean Session 与离线消息

- 所有指令/应答均为 **QoS 1**（至少送达一次），设备需能处理**重复消息**。
- `clean session = true`：断线期间平台不替你保留消息（如控制指令、被踢通知只在重连后才会收到后续消息）。
- `clean session = false`：平台保留会话约 2 小时（`session_expiry=7200s`），断线期间的 QoS1 应答会在重连后补收。
- **建议**：需要可靠接收控制/被踢原因的设备使用 `clean session=false`，并在重连后重新订阅全部主题。

---

## 2. 通用约定（务必先读）

### 2.1 通配符

- `+`：匹配**单层**。示例 `wendao/test_001/+/ack` 能匹配 `data/ack`、`control/ack`、`ping/ack`。
- `#`：匹配**其后所有层**，只能放在末尾。示例 `wendao/test_001/#` 收下本人全部消息。
- 通配符只用于**订阅**，发布必须用完整主题。

### 2.2 顺序：先订阅，再发布 ⚠️

平台对上报/指令是 **QoS1 即时转发**，应答**不会等你订阅后再补发**。因此：

- 正确：**连接成功 → 订阅应答主题 → 再 publish 数据**。
- 错误：先 publish 再去订阅应答主题 → 应答已发出，你只会错过。

建议连接后一次性订阅本人全部需要的主题，再开始上报。

### 2.3 消息 `id`

- 每条请求带一个字符串 `id`（设备自行生成，需**全局/本次会话唯一**，可用 UUID、递增序号或时间戳+随机数）。
- 平台在应答里**原样回显 `id`**，设备据此把应答与请求配对。
- 设备必须本地缓存"已发送待应答"的 `id`，收到应答后再清除。

### 2.4 时间戳如何处理 ⚠️

平台对时间戳做统一归一（函数逻辑等价于下面规则）：

- 设备传 **秒级**时间戳（`< 1_000_000_000_000`）→ 平台自动 `× 1000` 转毫秒；
- 设备传 **毫秒级**时间戳（`≥ 1e12`）→ 原样使用；
- 传 `0` 或不传 → 记为 0。

**关键口径**：

- 数据**排序与展示的权威时间 = 平台服务器接收时刻**（毫秒）；设备自报的时间戳只记录在参考字段里，**不决定数据先后顺序**。
- 所以即使设备时钟偏差较大或时间戳传错，平台数据时间仍然正常，设备无需为此做校时也不会乱序。
- 建议有 RTC 的设备按毫秒上报 `ts`；没有可靠时钟也**不要随意编造未来时间**（字段会被如实记录为参考值）。

### 2.5 Tags 值必须是数字

- 数据上报和控制指令里的 `tags` 是 `map[string]float64`：**键任意字符串，值必须是 JSON 数字**（不能是 `"26.5"` 字符串、布尔、null）。
- 温度、湿度、电压、开关量等都用数字表示（开关可用 `0/1`）。

### 2.6 非法 JSON 不回应

如果平台无法把 payload 解析为合法 JSON（或字段类型不符），会**直接丢弃，不返回任何应答**。
设备若在超时内未收到应答，应判定本次失败并按需重发（建议带退避，避免无间隔重发）。

---

## 3. 数据上行（设备核心）

> 设备主动上报，最常见动作。设备只负责：**publish 数据 → 等待 `data/ack`**。

### 3.1 主题

| 方向     | 主题                           | 动作                    |
| -------- | ------------------------------ | ----------------------- |
| 设备上报 | `wendao/{deviceID}/data`     | **publish，QoS1** |
| 上报应答 | `wendao/{deviceID}/data/ack` | **subscribe**     |

### 3.2 推荐订阅（二选一）

订阅**应答字面主题**：

```
wendao/test_001/data/ack
```

或更省事，用**通配订阅**一次收下本人所有应答（推荐）：

```
wendao/test_001/#
```

分层通配（只收各类 ack）：

```
wendao/test_001/+/ack
```

### 3.3 报文示例

**最简报文**（只要 `id` 和 `tags` 即可，其余字段可省略）：

```json
{
  "id": "msg-1001",
  "tags": {
    "temperature": 26.5,
    "voltage": 3.32
  }
}
```

**推荐报文**（带时间戳与固件版本，便于平台记录）：

```json
{
  "id": "msg-1001",
  "ts": 1727000001000,
  "version": "1.0.3",
  "tags": {
    "temperature": 26.5,
    "humidity": 58.2,
    "voltage": 3.32,
    "rssi": -67,
    "power": 1
  }
}
```

**完整可选（补首次启动时间）**：在推荐报文基础上额外加 `"first_ts": 1726999000000`，用于平台记录设备首启时间（重启时会更新）。

### 3.4 字段说明

| 字段         | 类型   | 必填 | 说明                                              |
| ------------ | ------ | ---- | ------------------------------------------------- |
| `id`       | string | 是   | 消息唯一 ID，应答原样回显                         |
| `ts`       | number | 否   | 设备时间戳，秒/毫秒均可自动归一（仅参考，见 2.4） |
| `version`  | string | 否   | 固件版本号                                        |
| `first_ts` | number | 否   | 首次启动时间，秒/毫秒均可                         |
| `tags`     | object | 是   | 业务数据，值必须是数字                            |

### 3.5 平台应答（在 `data/ack` 上）

成功：

```json
{ "id": "msg-1001", "code": 0, "msg": "ok" }
```

设备未注册或被禁用：

```json
{ "id": "msg-1001", "code": 1, "msg": "device not registered" }
```

> 上报成功后，平台会把设备置为**在线**并更新最后活跃时间。
> 若平台未应答（非法 JSON 等情况），设备按超时处理。

---

## 4. 平台指令下行（设备核心）

> 平台/管理员主动下发指令。设备只负责两件事：**订阅接收 `control` → 处理后回执 `control/ack`**。
> 设备回执的格式决定了平台上该指令最终「成功/失败」状态。

### 4.1 主题

| 方向             | 主题                                   | 动作                              |
| ---------------- | -------------------------------------- | --------------------------------- |
| 平台下发指令     | `wendao/{deviceID}/control`          | **subscribe**               |
| 设备回执         | `wendao/{deviceID}/control/ack`      | **publish，QoS1**           |
| 平台对回执的确认 | `wendao/{deviceID}/control/ack/resp` | **subscribe**（可选但推荐） |

### 4.2 推荐订阅

只收指令：

```
wendao/test_001/control
```

一次收「指令 + 各类下行 + 回执确认」：

```
wendao/test_001/#
```

或精确匹配回执确认：

```
wendao/test_001/control/ack/resp
```

### 4.3 平台下发的报文（设备接收）

平台只在设备**在线**时下发。报文形如：

```json
{
  "id": "8f4b1c2e-9d3a-4f6b-8c21-5a7e9d0b1234",
  "ts": 1727000001500,
  "tags": {
    "shutdown": 1,
    "mode": 2
  }
}
```

- `id`：本次指令 ID，**设备回执必须原样带回**。
- `tags`：指令参数和目标值，值为数字。设备按各个 key 的业务含义执行（例如 `shutdown=1` 表示关机）。

设备处理流程建议：

1. 收到 `control`，先记下 `id`；
2. 逐条执行 `tags` 中的指令；
3. 无论整体成功/失败/部分失败，都向 `control/ack` 回一条结果，并带回该 `id`。

### 4.4 设备回执报文（publish 到 `control/ack`）

**最简报文**（执行成功）：

```json
{
  "id": "8f4b1c2e-9d3a-4f6b-8c21-5a7e9d0b1234",
  "code": 0,
  "msg": "ok"
}
```

**推荐报文（带可读说明/失败原因）**：

```json
{
  "id": "8f4b1c2e-9d3a-4f6b-8c21-5a7e9d0b1234",
  "code": 0,
  "msg": "shutdown accepted"
}
```

执行失败时把 `code` 设为非 0，`msg` 写原因：

```json
{
  "id": "8f4b1c2e-9d3a-4f6b-8c21-5a7e9d0b1234",
  "code": 1,
  "msg": "actuator busy"
}
```

### 4.5 字段说明

| 字段     | 类型   | 必填 | 说明                                                             |
| -------- | ------ | ---- | ---------------------------------------------------------------- |
| `id`   | string | 是   | **必须等于收到的指令 id**，否则平台无法记账                |
| `code` | number | 是   | `0`=成功（平台置指令 success）；非 `0`=失败（平台置 failed） |
| `msg`  | string | 否   | 处理结果/失败原因                                                |

### 4.6 平台对回执的二次确认（`control/ack/resp`）

平台收到你的 `control/ack` 后，会在 `control/ack/resp` 上再回一条，端侧据此确认平台已受理：

```json
{ "id": "8f4b1c2e-9d3a-4f6b-8c21-5a7e9d0b1234", "code": 0, "msg": "ok" }
```

- `code=0`：回执已受理。**同一 `id` 重复回执是幂等的**，仍返回 `code:0`（设备重发安全）。
- `code=3`：`id` 缺失，或该 id 平台没有对应指令（`unknown msg id` / `id required`）。

### 4.7 不回执会怎样

- 设备若一直不回 `control/ack`，平台在约 **30 秒**后把该指令标记为 `timeout`。
- 因此即使执行失败，也**务必回一条带非 0 code 的 ack**，不要沉默。

---

## 5. 在线 ping 探活

> 仅当设备的在线判定采用 **ping 模式**时需要（普通 `connection` 模式靠 MQTT 连接即可，无需处理）。
> ping 模式下平台周期性点名，设备应答以证明可达。

### 5.1 主题

| 方向     | 主题                           | 动作                    |
| -------- | ------------------------------ | ----------------------- |
| 平台探活 | `wendao/{deviceID}/ping`     | **subscribe**     |
| 设备应答 | `wendao/{deviceID}/ping/ack` | **publish，QoS1** |

### 5.2 平台报文与设备应答

平台发送：

```json
{ "id": "ping-556677", "ts": 1727000002000, "tags": {} }
```

设备应答（带回原 `id`，`code=0` 表示可达）：

```json
{ "id": "ping-556677", "code": 0, "msg": "ok" }
```

- 超过设备的离线时限仍未收到 `ping/ack`，平台将判设备离线。
- ping 不写入控制日志，仅用于在线判定；休眠设备不适合 ping 模式，建议改 `connection`/`report`。

---

## 6. OTA 固件升级

> 平台下发升级通知，设备下载并刷机，期间上报进度，结束回报结果。

### 6.1 主题

| 方向     | 主题                               | 动作                    |
| -------- | ---------------------------------- | ----------------------- |
| 升级通知 | `wendao/{deviceID}/ota`          | **subscribe**     |
| 升级进度 | `wendao/{deviceID}/ota/progress` | **publish，QoS1** |
| 升级结果 | `wendao/{deviceID}/ota/ack`      | **publish，QoS1** |

### 6.2 平台升级通知（设备接收）

```json
{
  "id": "ota-9001",
  "type": "ota",
  "version": "1.0.4",
  "url": "https://pannel.wendaoiot.com/firmware/xxx_v1.0.4.bin",
  "md5": "d41d8cd98f00b204e9800998ecf8427e",
  "size": 248320
}
```

- `url` 一定是 **https**；设备务必先按 `md5`/`size` 校验下载完整性，再刷机。
- `id` 用于关联后续进度与结果。

### 6.3 进度上报（`ota/progress`）

```json
{
  "id": "ota-9001",
  "type": "ota",
  "progress": 60,
  "status": "downloading"
}
```

- `progress`：0–100 整数；`status`：`downloading`（下载中）或 `installing`（刷写中）。

### 6.4 结果上报（`ota/ack`）

成功：

```json
{
  "id": "ota-9001",
  "type": "ota",
  "code": 0,
  "msg": "ok"
}
```

失败（`code` 非 0，`msg` 写原因，如校验失败/写 Flash 失败）：

```json
{ "id": "ota-9001", "type": "ota", "code": 1, "msg": "md5 mismatch" }
```

---

## 7. 设备间通信 D2D（可选）

> 一台设备经平台给另一台/一批设备发消息。发件方收投递回执，收件方在收件箱取信。

### 7.1 主题

| 方向       | 主题                           | 动作                    |
| ---------- | ------------------------------ | ----------------------- |
| 发送消息   | `wendao/{deviceID}/peer`     | **publish，QoS1** |
| 投递回执   | `wendao/{deviceID}/peer/ack` | **subscribe**     |
| 我收到的信 | `wendao/{deviceID}/inbox`    | **subscribe**     |

### 7.2 发送报文（publish `peer`）

```json
{
  "id": "peer-3001",
  "ts": 1727000003000,
  "to": "test_002",
  "type": "notify",
  "payload": { "text": "hello" }
}
```

`to` 目标语义：

| `to` 取值      | 含义     | 范围/限制                                        |
| ---------------- | -------- | ------------------------------------------------ |
| `"test_002"`   | 点对点   | 精确投递；**唯一支持跨租户白名单**的模式   |
| `"a,b,c"`      | 多播     | 逗号分隔，自动去重；仅限同租户                   |
| `"*"`          | 租户广播 | 同租户全部**在线且启用**设备，自动排除自己 |
| `"project:12"` | 项目广播 | 项目 12 内全部在线启用设备，跨租户项目拒绝       |
| `""` / 缺失    | 非法     | 回执 code=3                                      |

### 7.3 投递回执（`peer/ack`）

```json
{ "id": "peer-3001", "code": 0, "msg": "delivered 1/1" }
```

- 多播/广播：任一成功即 `code=0`，`msg` 形如 `delivered 2/3`；全投在线才可能全成功。
- 平台**只做在线投递，不做离线暂存**，目标不在线直接 `code=5`，业务侧收到后自行重发。
- 回执码：`0` 已投递；`3` 缺 id/to；`5` 目标离线；`6` 跨租户且无白名单；`7` 目标不存在。

### 7.4 收件箱（`inbox`，作为接收方）

```json
{
  "id": "peer-3001",
  "from": "test_001",
  "type": "notify",
  "payload": { "text": "hello" },
  "ts": 1727000003100
}
```

---

## 8. 被踢通知与连接异常排查

> 设备建议**长期订阅** `wendao/{deviceID}/kicked`，平台在主动断开前会发一条 QoS1 通知，告诉设备"为什么断"。

### 8.1 订阅

```
wendao/test_001/kicked
```

或本人全量：

```
wendao/test_001/#
```

### 8.2 通知报文

```json
{
  "reason": "new_login",
  "msg": "同一设备的新连接已通过认证，本连接被平台断开（互踢）",
  "ts": 1727000004000
}
```

`reason` 对照表：

| reason        | 含义                                                 | 设备应如何理解                                                             |
| ------------- | ---------------------------------------------------- | -------------------------------------------------------------------------- |
| `new_login` | 同一设备 ID/密钥在别处建立了新连接（**互踢**） | 本连接被新连接挤下线。**切勿无退避自动重连**，否则与另一端互相反复踢 |
| `disabled`  | 设备被管理员禁用                                     | 联系管理员/等待启用，不要立即重连                                          |

- 通知在**断开之前**发出。`clean session=false` 时若错过，重连后仍可能补收该条。
- 正常行为：同设备两端反复重连会互相踢——固件必须带退避/退避后人工介入。

### 8.3 "认证失败(连不上)" 与 "连上后被踢" 的区别

- **认证失败**：TCP/连接阶段就被拒，MQTTX/客户端收到 CONNACK 错误码，**没有进入业务**。
- **被踢**：先成功连上（可能已收发），随后收到 `kicked` 再断开——这是主动的会话管理，不是凭据错误。
- 看到 `kicked` 通知就说明凭据没问题，是会话/状态原因；什么通知都没有却连不上，才查认证。

### 8.4 rc=135（Not authorized）自查清单 ⚠️

外部明文连接被拒时常见返回 135。按顺序排查：

1. **用户名串**：是否为正确的设备 ID，**大小写完全一致**？（大小写敏感）
2. **密钥**：是否为该设备当前的 `device_secret`？重置后旧密钥立即失效，不能新建连接。
3. **设备状态**：设备是否被**禁用**？（禁用同样拒绝）
4. **主题**：是否发布/订阅了非本人或白名单外的主题？平台策略是 `no_match=deny` 且 `deny_action=disconnect`，违规即断开。
5. **占位符**：是否把 `{deviceID}` 连花括号原样写进主题？（如 `wendao/{test_001}/...`）花括号必须去掉，换成真实 ID。
6. **平台侧**：以上都正确却偶发超时，可能是平台认证回调临时不可用或网络抖动，稍后重试；持续出现请联系管理员查服务日志。

其它 CONNACK 参考：**134** 多为未带用户名/匿名被禁；**135** 为用户名/密钥/权限不被接受。

---

## 9. 订阅与发布权限汇总

平台授权口径：

- **发布（publish）= 严格白名单**：只允许下列"设备可发布"的后缀；命中其它一律拒绝并断开。
- **订阅（subscribe）= 本人前缀全放行**：第二段 == 本人设备 ID 时，字面主题与**任意通配符**都可订阅，无越权面。
- 他人前缀、原样花括号占位符、`$SYS` 系统主题等一律拒绝。

| 主题后缀（相对`wendao/{deviceID}/`） |   publish   | subscribe |
| -------------------------------------- | :----------: | :-------: |
| `data`                               |      ✅      |    ✅    |
| `data/ack`                           | ❌（平台发） |    ✅    |
| `control`                            | ❌（平台发） |    ✅    |
| `control/ack`                        |      ✅      |    ✅    |
| `control/ack/resp`                   | ❌（平台发） |    ✅    |
| `ping`                               | ❌（平台发） |    ✅    |
| `ping/ack`                           |      ✅      |    ✅    |
| `ota`                                | ❌（平台发） |    ✅    |
| `ota/progress`                       |      ✅      |    ✅    |
| `ota/ack`                            |      ✅      |    ✅    |
| `peer`                               |      ✅      |    ✅    |
| `peer/ack`                           | ❌（平台发） |    ✅    |
| `inbox`                              | ❌（平台发） |    ✅    |
| `kicked`                             | ❌（平台发） |    ✅    |
| `status`（遗嘱）                     |      ✅      |    ✅    |
| 本人前缀任意通配（如`#`、`+/ack`） |      —      |    ✅    |

> 引导连接（一型一密，username 形如 `SN&ProductKey`）只能使用 `wendao/register/{sn}/req`（publish）与
> `wendao/register/{sn}/reply`（subscribe），其余主题全部拒绝。普通设备无需关心。

---

## 10. 附录：错误码与自检清单

### 10.1 错误码总表

| code | 常量                | 含义           | 典型出现                         |
| ---- | ------------------- | -------------- | -------------------------------- |
| 0    | Success             | 成功           | 上报/控制/Ping/OTA/D2D 成功      |
| 1    | DeviceNotRegistered | 设备未注册     | 上行应答；设备在平台不存在       |
| 2    | TagNotConfigured    | 标签未配置     | 标签相关                         |
| 3    | ParamError          | 参数错误       | 缺 id/to；控制回执 id 平台无记录 |
| 4    | AlreadyActivated    | 一型一密已激活 | 引导重复注册                     |
| 5    | DeviceOffline       | 目标离线       | D2D 目标不在线                   |
| 6    | Forbidden           | 越权           | D2D 跨租户且无白名单             |
| 7    | PeerNotFound        | 目标不存在     | D2D 找不到目标设备               |

> CONNACK 层：134=用户名/匿名问题，135=认证/授权不通过；业务层 `code` 在各应答 JSON 内。

### 10.2 最小自检清单（MQTTX / paho 均可）

1. 用设备 ID + 密钥连 `pannel.wendaoiot.com:1883`（先联调）或 `:8883`（正式）；
2. 先订阅 `wendao/<id>/#`；
3. 向 `wendao/<id>/data` 发**最简报文**，应在 `data/ack` 收到 `{"code":0}`；
4. 平台向本设备下发一条 `control`，设备回 `control/ack {"id":<原样>,"code":0}`，并在 `control/ack/resp` 收到 `{"code":0}`；
5. 用同一设备凭据再开一个连接，旧连接应在 `kicked` 收到 `reason:new_login`；
6. 反例：带花括号占位符订阅或发他人主题，应被拒绝/断开（rc=135）。

### 10.3 端侧实现要点速记

- 连接成功 → **先全部订阅** → 再上报；
- 每条请求唯一 `id`，本地缓存待应答，靠回显 `id` 配对；
- 收到 control/ping → **必应答**，失败也回非 0，别沉默；
- 指令均按可能重复处理（QoS1 / 幂等）；
- tags 值只用数字；时间戳缺时钟可不传或传毫秒，平台以接收时间为准；
- 订阅 `kicked`，被踢时带退避，不做无间隔重连。
