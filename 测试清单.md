# 测试清单

## 一、环境准备

### 1.1 依赖检查
- [ ] MySQL 8.0 已启动（默认 127.0.0.1:3306，root 无密码）
- [ ] EMQX 或 Mosquitto 已启动（默认 127.0.0.1:1883，无认证）
- [ ] Go 1.22+ `go version`
- [ ] Node.js 18+ `node -v`

### 1.2 创建数据库
```bash
mysql -u root -e "CREATE DATABASE IF NOT EXISTS wendaoiot DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci"
```

### 1.3 修改配置（如需要）
编辑 `server/config.yaml`，修改数据库密码或 MQTT 地址。

---

## 二、编译与启动

### 2.1 编译后端
```bash
cd server
go build -o server.exe ./cmd/server/
```

### 2.2 启动后端
```bash
cd server
./server.exe
# 预期输出: MQTT connected and subscribed / server start on :8080
```

### 2.3 健康检查
```bash
curl http://localhost:8080/api/v1/health
# 预期: {"code":0,"msg":"ok","data":{"status":"ok","time":..."}}
```

### 2.4 安装并启动管理后台
```bash
cd web-admin
npm install
npm run dev
# 浏览器打开 http://localhost:3000
```

### 2.5 （可选）启动设备模拟器
```bash
cd tools/simulator
go run . -id ESP32-001
# 预期: 每5秒输出 "上报数据: temp=... humidity=... voltage=..."
```

### 2.6 （可选）启动 C端 H5
```bash
cd web-app
npm install
npm run dev:h5
# 浏览器打开 http://localhost:3001?project_id=1
```

---

## 三、冒烟测试（核心流程）

### 3.1 登录
- [ ] 打开 http://localhost:3000 → 登录页
- [ ] 登录 `admin / admin123` → 角色选"超级管理员" → 进入仪表盘
- [ ] 仪表盘显示：租户数=0, 项目数=0, 设备总数=0

### 3.2 创建租户
- [ ] 侧边栏 → 租户管理 → 新建租户 → 输入"测试公司" → 确定
- [ ] 列表显示一条记录，弹出 "admin_user: 测试公司_admin, admin_pwd: 123456"
- [ ] 仪表盘刷新 → 租户数变为 1

### 3.3 创建项目
- [ ] 项目管理 → 新建项目 → 选择"测试公司" → 输入"智慧农业" → 确定
- [ ] 列表显示一条记录

### 3.4 注册设备
- [ ] 设备管理 → 选择一个项目 → 新建设备
- [ ] 设备ID: `ESP32-001`，名称: `环境传感器` → 确定
- [ ] 状态显示"离线"（黄色标签）

### 3.5 配置设备标签
- [ ] 设备列表点击"标签配置" → 添加标签配置
- [ ] 标签键: `temp`，接口: `AI0`，公式: `x*0.1` → 保存
- [ ] 再添加: `humidity` / `AI1` / （留空）
- [ ] 再添加: `voltage` / `AIN2` / （留空）

### 3.6 绑定项目标签
- [ ] 项目管理 → 标签 → 绑定标签
- [ ] 标签键: `temp` → 保存
- [ ] 同样绑定 `humidity` 和 `voltage`

### 3.7 启动设备模拟器（如果还没启动）
- [ ] 新终端: `cd tools/simulator && go run . -id ESP32-001`
- [ ] 设备管理页面 → 设备状态变为"在线"（绿色）

### 3.8 查看设备数据
- [ ] 设备列表 → 点击"数据"
- [ ] 表格显示数据行，data 字段含 `temp`, `humidity`, `voltage` 及计算后的值（temp 应 ≈ 原始值 × 0.1）
- [ ] 分页控件正常（数据 > 20条时出现）

### 3.9 控制指令
- [ ] 设备列表 → 点击"控制"
- [ ] 标签键输入 `relay1`，值输入 1 → 发送指令
- [ ] 提示"指令发送成功，消息ID: xxx"
- [ ] 设备模拟器终端输出 "收到控制指令: relay1=1"
- [ ] 设备模拟器终端输出 "已回复控制ACK: code=0"

---

## 四、功能测试

### 4.1 多租户隔离
- [ ] 退出登录 → 用 `测试公司_admin / 123456` 登录，角色选"租户管理员"
- [ ] 仪表盘看不到"租户数"卡片（仅超管可见）
- [ ] 项目管理自动显示"测试公司"的项目
- [ ] 退出 → 重新用超管登录 → 确认租户管理员无法看到其它租户数据

### 4.2 公式引擎
- [ ] 查看设备数据页 → 原始上报值 temp=25.3，存储值应为 25.3×0.1=2.53
- [ ] 编辑设备标签，公式改为 `x*1.8+32` → 下一次上报值会变化

### 4.3 C端 H5
- [ ] 打开 http://localhost:3001 → 登录 `测试公司_admin / 123456` 角色选 tenant_admin
- [ ] 进入项目监控页 → 显示 temp/humidity/voltage 卡片
- [ ] 卡片实时刷新（3秒一次）
- [ ] 控制输入框输入值 → 发送 → 设备模拟器响应
- [ ] 下拉刷新正常

### 4.4 仪表盘统计
- [ ] 超管登录 → 仪表盘 → 数字卡片正常
- [ ] 设备状态分布正确（在线/离线比例）

### 4.5 OTA 升级
- [ ] 固件管理 → 上传固件
  - 名称: `测试固件v2`，版本: `2.0.0`
  - 地址: `http://example.com/firmware.bin`（可以用假地址）
- [ ] OTA升级 → 创建升级任务 → 选择固件 → 目标类型选"单设备" → ID: `ESP32-001`
- [ ] 点击任务查看日志 → 自动刷新显示进度（pending → downloading → installing → success）
- [ ] 设备模拟器终端输出 OTA 过程日志

### 4.6 项目聚合接口
- [ ] `curl -H "Authorization: Bearer <token>" http://localhost:8080/api/v1/projects/1/data`
- [ ] 返回 JSON 含 project_id 和 tags 数组，每个 tag 含 devices 数组和最新 value

---

## 五、边界与异常测试

### 5.1 认证
- [ ] 未登录直接访问 `http://localhost:3000/dashboard` → 自动跳转登录页
- [ ] 用错误密码登录 → 提示 "username or password incorrect"
- [ ] 超管选"租户管理员"角色登录 → 提示 "role mismatch"

### 5.2 设备上报异常
- [ ] 修改模拟器设备ID为未注册ID → 启动 → 服务端回复 code=1（设备未注册）
- [ ] 设备管理将设备状态设为"未激活" → 模拟器从此设备上报 → 服务端回复 code=1

### 5.3 空状态
- [ ] 新租户登录 → 仪表盘数字全为 0
- [ ] 未选项目 → 设备管理页显示"请先选择一个项目"
- [ ] 无数据的设备 → 数据页显示"暂无数据"
- [ ] 无标签的项目 → C端显示"暂无设备数据"

### 5.4 前后端分离
- [ ] 停止后端 → 前端页面操作 → 弹窗提示网络错误
- [ ] 重启后端 → 登录过期 → 自动跳转登录页

---

## 六、编译清单

| 模块 | 命令 | 预期 |
|------|------|------|
| server | `cd server && go build ./cmd/server/` | 无错误 |
| server test | `cd server && go test ./...` | 全部通过 |
| web-admin | `cd web-admin && npm run build` | 构建成功 |
| web-app H5 | `cd web-app && npm run build:h5` | 构建成功 |
| web-app MP | `cd web-app && npm run build:mp-weixin` | 构建成功 |
| simulator | `cd tools/simulator && go build .` | 无错误 |
